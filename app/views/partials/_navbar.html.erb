<nav class="w-full bg-main-700">
  <div class="container mx-auto py-6 px-4">
    <ul class="w-full flex justify-between items-center gap-2 text-lg">
      <li class="w-1/6 text-white"><button type="button">M</button></li>
      <li class="w-3/6 text-white text-semibold uppercase text-center">CATEGORIES</li>
      <li class="w-2/6 text-white text-right">
        <% if user_signed_in? %>
          <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete } %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
